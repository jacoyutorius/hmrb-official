# hmrb-official

> Hamamatsu.rb website

## Develop on Docker

run container

```
$ docker-compose build
$ docker-compose up -d 
$ docker-compose exec vue_app sh
```

start dev-server

```
/app # yarn install
/app # yarn start
```

http://0.0.0.0:8080